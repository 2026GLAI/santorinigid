/**
 * Отзывы клиентов Владимира Лунгу.
 *
 * СОДЕРЖИМОЕ ЖИВЁТ В reviews.json. Его пишут ДВОЕ:
 *  • владелец через админку /admin (правка, удаление);
 *  • Worker модерации (worker/review-worker.js) — дописывает одобренный
 *    отзыв в начало массива reviews и удаляет по кнопке «снять с сайта».
 * Этот файл — только типы и вычисления. Тексты сюда руками не вписывать.
 * (Миграция 06.08.2026, собранный сайт сверен байт-в-байт.)
 *
 * ИСТОЧНИКИ (у каждого отзыва свой + прямая ссылка на оригинал):
 *  • Tourister.ru — https://vladimir-santorini.tourister.ru/responses
 *  • NeedGuide    — https://needguide.ru/view_guide.php?user_id=1230
 * Собрано 2026-07-25.
 *
 * ЧЕСТНОСТЬ — главное правило этих данных:
 *  1. Тексты сверены с оригиналами СИМВОЛ В СИМВОЛ (15/15 с Tourister).
 *     Опечатки авторов сохранены — это их слова, править их мы не вправе.
 *  2. НЕТ оценок в звёздах. Их нет ни на одной площадке: Tourister не хранит
 *     рейтинг, у виджета NeedGuide стоит ratingOff: true. Поставить «5 звёзд»
 *     значило бы выдумать оценку, которую человек не давал.
 *  3. datePublished — РЕАЛЬНАЯ дата публикации на площадке. Дата поездки
 *     (tripDate) хранится отдельно: это разные вещи.
 *  4. Ответ Владимира (reply) стоит ТОЛЬКО у тех отзывов, под которыми он
 *     реально его написал (все — на NeedGuide).
 *  5. Дубли убраны: три человека оставили отзывы на обеих площадках —
 *     оставлена версия с полными данными.
 *  6. dislikes — отдельное поле площадки «Что не понравилось».
 *
 * Политика Google по «отзывам о себе» — docs/harvest/reviews-research.md
 *
 * КАК ДОБАВИТЬ ОТЗЫВ: объект в массив reviews в reviews.json (порядок не
 * важен — сортировка по дате автоматическая). Обязательны: id, author,
 * text, datePublished (ГГГГ-ММ-ДД), source, sourceUrl.
 */
import data from './reviews.json';

export interface Review {
  /** Идентификатор: tr-* с Tourister, ng-* с NeedGuide, own-* — оставленные на нашем сайте. */
  id: string;
  author: string;
  /** Дословный текст отзыва. */
  text: string;
  /** Месяц и год поездки, если площадка их указывает. */
  tripDate: string;
  /** Состав компании: «семейная пара, 2 человека» и т.п. */
  tripWith: string;
  /** Дата публикации словами, как на площадке. */
  published: string;
  /** Дата публикации для сортировки и разметки, ГГГГ-ММ-ДД. */
  datePublished: string;
  /** Площадка-источник. */
  source: string;
  /** Прямая ссылка на оригинал отзыва. */
  sourceUrl: string;
  /** Ответ Владимира — только там, где он реально его оставил. */
  reply?: string;
  /** Поле площадки «Что не понравилось» — хранится отдельно от текста. */
  dislikes?: string;
  /** Фотографии, приложенные к отзыву (для отзывов с нашего сайта). */
  photos?: string[];
}

/** Отзывы, отсортированные по дате публикации: свежие сверху. */
export const REVIEWS: Review[] = [...(data.reviews as Review[])].sort((a, b) =>
  b.datePublished.localeCompare(a.datePublished),
);

/** Площадки, откуда собраны отзывы. */
export const REVIEW_SOURCES = [
  { name: 'Tourister.ru', url: 'https://vladimir-santorini.tourister.ru/responses' },
  { name: 'NeedGuide', url: 'https://needguide.ru/view_guide.php?user_id=1230' },
] as const;

/** Сколько отзывов показываем сразу; остальные — под кнопкой (без JS-подгрузки). */
export const REVIEWS_VISIBLE = 6;

/*
 * Диапазона годов (REVIEW_YEARS) здесь БОЛЬШЕ НЕТ — убран 06.08.2026
 * по слову владельца: сводка «2014–2026» при скромном числе отзывов
 * подчёркивала, что их мало (отзывы годами копились у площадок-партнёров,
 * а не у него). Даты честно стоят у каждого отзыва. Не возвращать.
 */
