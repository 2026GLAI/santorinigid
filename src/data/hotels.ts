/**
 * 14 отелей с партнёрскими ссылками.
 *
 * СОДЕРЖИМОЕ ЖИВЁТ В hotels.json — его правит владелец через админку
 * /admin (миграция 06.08.2026, собранный сайт сверен байт-в-байт,
 * все 14 партнёрских ссылок сверены списком до и после).
 * Этот файл — только типы поверх данных.
 *
 * ⚠️ КРИТИЧНО: ссылки идут через tourister.ru/ostr/<код> — это партнёрская
 * программа владельца (его доход). URL сняты с живой страницы 2026-07-25 и
 * должны сохраняться символ в символ. Не «чинить», не сокращать, не заменять
 * на прямые ссылки ostrovok.ru (реально они ведут именно на Ostrovok —
 * проверено редиректом 06.08.2026, так и задумано).
 * Источник: docs/harvest/verbatim-content.md
 */
import data from './hotels.json';

export interface Hotel {
  name: string;
  area: string;
  description: string;
  image: string;
  /** Партнёрская ссылка. Менять только по прямому указанию владельца. */
  link: string;
}

export const HOTELS: Hotel[] = data.hotels;
