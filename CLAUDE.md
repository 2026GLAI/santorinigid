# Проект: santorinigid — клон сайта santorinivip.com

## Что это

Полный переезд личного сайта Владимира Лунгу (русскоязычный гид на Санторини, живёт там с 1995 года, гидом ~14 лет) с платформы Wix на собственный статический сайт. Цель — 100% независимость от платформ: результат = обычные HTML-файлы в собственности владельца, ноль ежемесячных платежей (остаётся только ежегодная оплата домена).

Текущий сайт: https://www.santorinivip.com/ (Wix, создан ~2012). Работает, просканирован 2026-07-17. Полная опись контента: `docs/site-content.md` — читать перед началом сборки.

## Утверждённые решения (обсуждены с владельцем 2026-07-17)

- **Стек**: Astro, чисто статическая сборка (без SSR). Владельцу объяснено и им принято: Astro — инструмент сборки, не платформа; готовый сайт от Astro не зависит.
- **Язык**: только русский (как оригинал). Мультиязычность — не закладывать, добавим позже при запросе.
- **URL-адреса**: сохранить 1:1 с текущим сайтом (`/tours`, `/vip-service`, `/vip-service/yacht-rent-santorini`, `/vip-service/helicopter-tours`, `/vip-service/santorini-hotels-2026`, `/contacts`) — чтобы не потерять 14 лет SEO-истории.
- **Адаптивность**: полноценная поддержка мобильных, планшетов и десктопов («сайт-приложение» по словам владельца — уточнить в начале сборки, нужен ли PWA-режим или достаточно отзывчивой вёрстки).
- **Дизайн**: максимально современный на 2026 год, быстрый (Core Web Vitals в зелёной зоне).
- **SEO + GEO**: JSON-LD (LocalBusiness, TouristTrip, FAQPage), Open Graph, sitemap.xml, robots.txt, llms.txt для ИИ-поисковиков.
- **Фото**: ✅ РАЗРЕШЕНО выгружать с Wix — владелец прямо разрешил 2026-07-25 («Все фотографии ты можешь так же и с этого сайта использовать»). Отменяет прежний запрет. 62 оригинала скачаны, лежат в `docs/harvest/images/` (вне git, см. `docs/harvest/image-urls.txt`), рабочие копии — в `src/assets/img/`. Владелец позже пришлёт фото лучшего качества на замену.
- **Тексты**: в `docs/site-content.md` — пересказ содержимого (получен через WebFetch-суммаризацию). Дословные тексты при сборке взять повторным сканированием сырого HTML страниц Wix (сайт ещё работает) либо запросить у владельца.
- **Форма контактов**: виджет Wix заменить на бесплатный сервис (Formspree или Cloudflare Worker), письма на santorinivip@gmail.com. Форма вторична — главный канал связи WhatsApp.
- **Инфраструктура владельца**: есть GitHub-аккаунт (использовать: репозиторий сайта + автодеплой в Cloudflare Pages) и Railway Hobby с другими проектами (для статики НЕ нужен — не тратить его ресурсы).
- **Хостинг**: Cloudflare Pages (бесплатный). Перенос DNS домена santorinivip.com с Wix — в самом конце, с перекрытием, без простоя; потребуется доступ владельца к аккаунту Wix.
- **Домены — стратегия двух доменов** (решено владельцем 2026-07-17, заменяет раннее решение «оставить только старый»):
  - **Главный домен нового сайта: `santorinigid.com`** — выбран владельцем (слитно, без дефиса). На 2026-07-17 свободен [проверено через rdap.verisign.com]. Зарегистрировать в Cloudflare Registrar (~$10/год) КАК МОЖНО РАНЬШЕ — свободное имя может занять кто угодно. Смысл имени: понятен русскоязычным клиентам («Санторини гид»).
  - **Старый santorinivip.com сохранить навсегда** (~$10/год): перенести от регистратора Wix в Cloudflare, настроить постраничные 301-редиректы на santorinigid.com (`/tours`→`/tours` и т.д.) + «Смена адреса» в Google Search Console. Это передаёт SEO-вес; честно предупреждён: просадка позиций на недели–пару месяцев при переезде — норма.
  - Правило ICANN: перенос домена от Wix невозможен 60 дней после регистрации/продления — проверить дату продления перед переносом.

## Подтверждённые факты о владельце (2026-07-25, от него лично)

- **Полное имя: Владимир Лунгу** (подтверждено на Tourister.ru и NeedGuide).
- **На острове с 1995 года.** Не «30 лет» — эта цифра из старого сайта была неточной.
- **Гидом работает ~14 лет** (совпадает с «опыт работы с 2012 года» в профиле NeedGuide).
- Стаж НЕ вписан цифрой в тексты: считается от года в `src/data/site.ts`
  (константы `ON_ISLAND_SINCE = 1995`, `GUIDE_SINCE = 2012`) и пересчитывается
  при каждой сборке. Есть функция склонения: 31 год / 32 года / 35 лет.
  **Не вписывать стаж цифрой руками** — через год он станет неверным.
- Профили-партнёры (в JSON-LD sameAs): Tourister.ru, NeedGuide.

## Владелец и стиль работы

- Владимир, не программист. Объяснять по-русски, простыми словами, без жаргона, с аналогиями.
- Требует проверенных фактов, не терпит фантазий — в проекте активны скиллы `no-fabrication` и `right-path`, вызывать их в начале работы.
- Ключевая ценность: независимость от платформ. Любое решение проверять вопросом «что будет, если этот сервис исчезнет».

## Статус (2026-07-26)

**Сайт собран, 11 страниц. В интернет не опубликован — ждёт домена и хостинга.**

Сборка: `npm run build` → `dist/`. Просмотр с телефона: `node preview-server.cjs`
(откроет на `http://<ip-компьютера>:4600`, телефон в той же сети Wi-Fi).

### Что сделано

- Дословный контент снят с живого сайта → `docs/harvest/verbatim-content.md`. **Читать его, а не `docs/site-content.md`** (тот — пересказ через WebFetch, содержит неточности).
- 62 оригинальных изображения скачаны и разложены по смыслу (не по алфавиту!) — соответствие «фото → объект» снято из DOM по alt-текстам.
- Astro 7 + Sharp, чистая статика.
- **11 страниц**: 6 старых URL 1:1 + `/about` + `/santorini` + `/reviews` + 404.
- SEO/GEO: 31 блок JSON-LD, sitemap, robots.txt, llms.txt, OG-картинка 1200×630.
- **22 отзыва** с двух площадок, сверены с оригиналами символ в символ.
- ИИ-чат — работает на встроенной базе фактов с сайта, без API-ключа и без платежей.
- **Скролл-эффекты** — появление блоков, каскад, параллакс, полоска прогресса. Без единой библиотеки: 0 КБ JavaScript.
- **Адаптивность** под 320px–3840px, включая монитор владельца 3440×1440.
- Форма отзыва с загрузкой до 3 фото + Cloudflare Worker для модерации (код готов, не подключён).

### Исправленные ошибки (не повторять!)

- ❌ **Раскладывать фото по алфавиту** — было, привело к тому, что у Perivolas стояло чужое фото. Соответствие брать ТОЛЬКО из alt-текстов/DOM исходного сайта.
- ❌ **Рисовать «5 звёзд» в отзывах** — на исходном сайте рейтинга нет, придумывать нельзя. Убрано.
- ❌ **Лозунг «100% реальные люди» рядом с настоящими цифрами** — выглядел как цифра. В плашках только проверяемые числа.
- ❌ **Называть месяцы сезона** — на сайте их нет. Чат теперь отправляет вопрос Владимиру.
- ❌ `import()` с переменной в пути — Astro не видит такие картинки на сборке. Использовать `getImg()` из `src/data/images.ts` (там `import.meta.glob`).
- ❌ **`max-width` в `ch` без `min(…, 100%)`** — на телефоне заголовок вылезал за край на 378px. Было в 15 местах.
- ❌ **Медиазапросы для ширины контейнера** — проигрывали в каскаде и не срабатывали вовсе. Ширина считается через `clamp` в токенах.
- ❌ **Правила с классом на `<html>` внутри компонента Astro** — не попадают в сборку. И даже вынесенные в `global.css` внутри `@layer` проигрывают стилям компонентов (те вне слоёв). Решение: либо писать вне `@layer` в конце `global.css`, либо задавать стиль скриптом.
- ❌ **`prefers-reduced-motion`, выключающий opacity** — часть текста становилась невидимой навсегда. Движение выключаем, проявление оставляем.
- ❌ **CSS-колонки + `max-height: 0`** — карточки раскладывались в строку шириной 29812px. Не совмещать.

### ⚠️ Браузерный инструмент даёт ложные показания

В конце сессии 2026-07-25 `mcp__Claude_Browser__javascript_tool` начал отдавать
замороженные значения: после четырёх пересборок с разным CSS выдавал одни и те же
координаты до пикселя, а на inline `visibility: hidden !important` отвечал
`visible`. Так не бывает.

**Как проверять надёжно:** читать собранные файлы в `dist/` (grep по CSS и HTML).
Визуальную проверку просить у владельца — у него живой телефон.

### Уточнённые факты (старая опись врала)

- Яхт **8**, а не 9. Есть точные цены по каждому судну (низкий/высокий сезон × 3 формата).
- Партнёрские ссылки идут через **`tourister.ru/ostr/<код>`**, а НЕ напрямую на ostrovok.ru. Это доход владельца — сохранять символ в символ.
- FAQ — 5 вопросов, не 6.
- **Отзывов 22, а не 3.** 15 с Tourister.ru (`vladimir-santorini.tourister.ru/responses`)
  + 7 с NeedGuide (владелец прислал текстом — скачать их нельзя, виджет Cackle
  грузит через JS). Все в `src/data/reviews.ts`, период 2014–2026.
  Тексты с Tourister **сверены с оригиналами символ в символ** (15/15).
  У 6 отзывов есть ответ Владимира — только там, где он реально его написал.
  Это и есть причина, почему на нашем сайте виджетов нет — всё в статическом HTML.
- **Звёзд в отзывах нет и быть не может**: (1) оценок нет в источниках
  (Tourister их не хранит, у Cackle `ratingOff: true`); (2) правило Google —
  страницы LocalBusiness/Organization с отзывами о себе не получают звёзды
  в выдаче [developers.google.com/search/docs/appearance/structured-data/review-snippet].
  Звёзды берутся из Google Business Profile — задача владельца.

## Очередь работ (согласована с владельцем 2026-07-25)

Порядок: сначала то, что меняет сам сайт, потом публикация, в конце надстройки.

### 🔴 Первое в следующей сессии

1. **Дождаться ответа Владимира по чату.** Он проверяет на телефоне четыре пункта:
   окно на весь экран / крестик закрывает / кнопка WhatsApp спрятана / после
   закрытия всё вернулось. Проверить визуально сам не смог — инструмент врал.
2. **Эстетика остальных страниц.** Дизайном пройдена только `/reviews`.
   Осталось: главная, экскурсии, яхты, вертолёты, отели, контакты, о Санторини, обо мне.
   Владелец сказал «надо поработать над эстетикой страниц» — уточнить, что именно
   не нравится, или предложить своё.

### 🟠 Дальше

3. **Тексты и факты**: подтвердить цены на яхты на сезон 2026; согласовать новые
   тексты на `/santorini` (вводный абзац и FAQ написаны от лица владельца);
   уточнить состав авиапарка (R44 / H125 / Bell 407).
4. **Фото от владельца**: гора Пророка Ильи (карточки на `/santorini` и `/tours`
   стоят без фото — намеренно, чужое ставить нельзя). Он обещал прислать оригиналы
   лучшего качества на замену выгруженным с Wix.
5. **Публикация**: GitHub Pages (владелец выбрал его, не Cloudflare Pages).
   Файлы готовы: `.github/workflows/deploy.yml`, `public/.nojekyll`.
   Репозиторий: `2026GLAI/santorinigid`, ветка `main`, remote подключён.
6. **Домен**: `santorinigid.com` свободен на 2026-07-25 [rdap.verisign.com].
   Владелец покупает на **Porkbun**, решил делать это последним. Предупреждён:
   имя может занять кто угодно. `santorinivip.com` — лок ICANN истёк, перенос возможен.

### 🟡 Надстройки (не блокируют запуск)

7. **Модерация отзывов через Worker** — код готов в `worker/review-worker.js`,
   инструкция в `docs/setup-reviews.md`. Владелец выбрал вариант «кнопка в письме».
   Почта для писем: **rusantorini@gmail.com**. Cloudflare-аккаунт у него есть.
   Пока не подключено — форма открывает WhatsApp, отзывы не теряются.
8. **ИИ-чат с настоящим ИИ** — нужен ключ API + Worker. Решение и оплата за владельцем.
9. **Google Business Profile и Яндекс.Бизнес** — единственный способ получить звёзды
   в выдаче. См. `docs/promotion-plan.md`.

### Инфраструктура: что решено

- **Хостинг: GitHub Pages** (выбор владельца 2026-07-25). Railway НЕ используем —
  там его другие проекты.
- **Домен: Porkbun** (выбор владельца). К GitHub Pages подключается без проблем.
- **Cloudflare** — только для Worker модерации отзывов, не для хостинга.

---

## Карта проекта

### Данные (правим их, а не разметку страниц)

| Файл | Что внутри |
|---|---|
| `src/data/site.ts` | Контакты, навигация, WhatsApp-ссылки, **стаж считается от года** (`ON_ISLAND_SINCE = 1995`, `GUIDE_SINCE = 2012`) |
| `src/data/reviews.ts` | 22 отзыва. Добавить новый — дописать объект в массив `ALL` |
| `src/data/yachts.ts` | 8 судов, цены, программы, «что включено» |
| `src/data/hotels.ts` | 14 отелей. ⚠️ Партнёрские ссылки `tourister.ru/ostr/` — доход владельца |
| `src/data/tours.ts` | Точки маршрута, условия, FAQ, галерея |
| `src/data/images.ts` | `getImg('имя.jpg')` — единственный правильный способ взять картинку |

### Ключевые компоненты

| Файл | Особенности |
|---|---|
| `src/layouts/BaseLayout.astro` | SEO-разметка, класс `js-on`, наблюдатель прокрутки со страховкой на 3 сек |
| `src/components/Reviews.astro` | Кладка в колонки, честные плашки-цифры |
| `src/components/ReviewCard.astro` | Текст в HTML **один раз**, подрезка через `line-clamp` |
| `src/components/ReviewForm.astro` | Загрузка до 3 фото. `REVIEW_API = ''` → включить Worker |
| `src/components/ChatWidget.astro` | База фактов, на телефоне на весь экран. `CHAT_API_URL = ''` → включить ИИ |
| `src/styles/global.css` | Токены, эффекты прокрутки. **В конце — правила вне `@layer`** (иначе проигрывают стилям компонентов) |

### Документация

- `ПРОЧТИ-МЕНЯ.md` — для владельца, простым языком
- `docs/harvest/verbatim-content.md` — дословные тексты с исходного сайта
- `docs/harvest/reviews-research.md` — почему нет звёзд, политика Google
- `docs/setup-reviews.md` — инструкция по Worker для владельца
- `docs/promotion-plan.md` — план продвижения
- `docs/harvest/reviews-all.json` — исходные данные отзывов

### Проверка перед сдачей

```bash
npm run build
```

Затем в `dist/`: битые ссылки, валидность JSON-LD, alt у картинок.
На 2026-07-26: 144 ссылки без битых, 31 JSON-LD валиден, 93 картинки с alt,
главная 115 КБ, сайт 15.5 МБ.
